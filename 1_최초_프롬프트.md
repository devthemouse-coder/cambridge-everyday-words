프로젝트명: cambridge-everyday-words

현재 React + TypeScript + Vite 프로젝트를 생성했다.

목표:
초등학생이 사용하는 Cambridge Everyday Words 영어단어 학습 웹앱을 만든다.

기술 스택:
- React
- TypeScript
- Vite
- Supabase
- GitHub Pages
- 모바일 우선 반응형 웹

중요한 개발 원칙:
- 최대한 단순하게 만든다.
- 불필요한 라이브러리를 추가하지 않는다.
- 과도한 상태관리 라이브러리를 사용하지 않는다.
- 컴포넌트와 기능을 적절하게 분리한다.
- TypeScript의 any 사용을 최소화한다.
- 기존 Supabase DB 구조를 임의로 변경하지 않는다.
- DB 구조가 현재 요구사항과 맞지 않는다고 판단되면 먼저 나에게 설명하고 승인받는다.
- 비밀번호나 인증 토큰을 console에 출력하지 않는다.
- npm run build가 성공해야 한다.

현재 Supabase DB는 이미 구축되어 있다.

테이블:

organizations
- id
- name
- created_at
- updated_at

profiles
- id
- username
- display_name
- role
- organization_id
- can_manage_rounds
- recovery_question
- recovery_hint
- email
- is_active
- created_at
- updated_at

word_books
- id
- organization_id
- title
- level
- created_by
- updated_by
- created_at
- updated_at

rounds
- id
- word_book_id
- round_number
- created_by
- updated_by
- created_at
- updated_at

words
- id
- round_id
- word_order
- english
- meaning
- created_by
- updated_by
- created_at
- updated_at

audit_logs
- id
- organization_id
- user_id
- entity_type
- entity_id
- action
- old_data
- new_data
- created_at

역할:
- SUPER_ADMIN
- PARENT
- TEACHER
- STUDENT

권한:
role과 회차관리 권한은 별개이다.

can_manage_rounds = true이면
STUDENT / PARENT / TEACHER 여부와 관계없이
회차 및 단어를 등록/수정/삭제할 수 있다.

SUPER_ADMIN은 학원에 소속되지 않는다.
SUPER_ADMIN의 organization_id는 NULL이다.

현재 총관리자:
- display_name: 관리자
- role: SUPER_ADMIN
- organization_id: NULL
- can_manage_rounds: true

현재 학원:
- name: 캠브리지 영어학원
- organization_id:
  1723e057-5aab-4f48-a1fd-fe1fa0a9fa97

회원가입 요구사항:

사용자가 입력하는 정보:

1. 아이디 - 필수
2. 비밀번호 - 필수
3. 비밀번호 확인 - 필수
4. 표시명칭 - 필수
5. 학원 - 필수
6. 계정 찾기 질문 - 필수
7. 계정 찾기 힌트 - 필수
8. 이메일 - 선택

중요:
사용자는 이메일이 없어도 가입할 수 있어야 한다.
어린 학생은 이메일 계정이 없을 수 있다.

계정 찾기 질문/힌트는
비밀번호 찾기 기능으로 사용하지 않는다.

현재 서비스에는 비밀번호 찾기 기능을 만들지 않는다.

계정 찾기 질문/힌트는
관리자가 계정을 식별하기 위한 보조 정보이다.

비밀번호를 profiles 테이블에 저장하지 않는다.
비밀번호는 Supabase Auth가 관리한다.

로그인:
- 아이디
- 비밀번호

두 가지 입력만 사용한다.

사용자에게 이메일 로그인을 요구하지 않는다.

Supabase Auth는 이메일/전화번호 기반 비밀번호 인증을 사용하므로,
사용자에게 이메일을 요구하지 않고 아이디 기반 로그인 UI를 제공하기 위한
내부 인증 식별자 처리 방법을 설계한다.

이 부분은 보안상 중요한 부분이므로
구현 전에 현재 Supabase 방식으로 가능한 가장 안전하고 단순한 방법을 검토한다.

절대로:
- 실제 사용자 비밀번호를 별도 DB에 저장하지 않는다.
- service_role key를 브라우저에 넣지 않는다.
- Supabase의 RLS를 우회하지 않는다.

화면:

1. 로그인
   - 아이디
   - 비밀번호
   - 로그인 버튼
   - 회원가입 버튼

2. 회원가입
   - 아이디
   - 비밀번호
   - 비밀번호 확인
   - 표시명칭
   - 학원 선택
   - 계정 찾기 질문
   - 계정 찾기 힌트
   - 이메일 선택
   - 가입 버튼

3. 로그인 후 홈
   - 표시명칭
   - 학원명
   - 단어장 목록
   - 로그아웃

4. 아직 OCR은 만들지 않는다.
5. 아직 문제 출제 기능은 만들지 않는다.
6. 아직 연속회차 기능은 만들지 않는다.
7. 아직 관리자 페이지 전체를 만들지 않는다.

모바일 UI:
- 스마트폰을 최우선으로 한다.
- 초등학생이 쉽게 누를 수 있도록 버튼을 충분히 크게 만든다.
- 한국어 UI를 사용한다.
- 영어 단어는 영어로 표시한다.

작업 순서:

1. 현재 프로젝트 구조를 먼저 분석한다.
2. package.json을 확인한다.
3. 필요한 최소 패키지만 설치한다.
4. Supabase client를 만든다.
5. .env.local을 사용한다.
6. 로그인/회원가입 구조를 만든다.
7. 인증 상태를 새로고침해도 유지한다.
8. 로그인한 사용자의 profiles를 조회한다.
9. SUPER_ADMIN과 일반 사용자를 구분한다.
10. organization_id를 기준으로 학원 데이터를 구분한다.
11. RLS를 신뢰하고 클라이언트에서 보안 검사를 대신하지 않는다.
12. npm run build를 실행한다.
13. 오류가 있으면 수정한다.

중요:
작업을 한 번에 전부 구현하려 하지 말고,
먼저 현재 프로젝트 구조와 Supabase 인증 구현 방법을 분석한 뒤
나에게 계획을 설명하고 진행한다.