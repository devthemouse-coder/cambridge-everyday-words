분석 내용 확인했습니다.

전체적인 방향은 승인합니다.
다만 구현 전에 아래 조건을 추가해서 진행해주세요.

1. Supabase Auth의 synthetic email 방식을 사용합니다.

사용자가 입력한 username을 기반으로
내부 인증용 이메일을 생성합니다.

예:
username = minsu01
internal auth email = minsu01@no-email.cambridge

이 이메일은 실제 사용자에게 보여주지 않습니다.

실제 사용자가 입력하는 email은 profiles.email에만 선택적으로 저장합니다.

2. username은 가입 후 변경할 수 없도록 합니다.

username은 인증용 synthetic email과 연결되므로
프로필 수정 기능에서 username 변경 기능을 제공하지 않습니다.

display_name은 변경할 수 있습니다.

3. username 규칙을 추가합니다.

- 필수
- 4~20자
- 영문 소문자, 숫자, _, - 만 허용
- 공백 허용하지 않음
- 한글 허용하지 않음
- 중복 불가

회원가입 시 프론트엔드에서 검사하고
DB의 UNIQUE 제약도 최종 방어선으로 사용합니다.

4. Supabase Auth의 이메일 확인 기능은 사용하지 않는 방향으로 구현합니다.

현재 서비스에서는 실제 이메일이 필수가 아니므로
합성 이메일로 이메일 인증을 요구하면 안 됩니다.

5. 실제 이메일을 입력한 사용자에게도
현재 단계에서는 이메일 인증이나 비밀번호 찾기 기능을 만들지 않습니다.

6. 비밀번호는 절대로 profiles에 저장하지 않습니다.
Supabase Auth에서만 관리합니다.

7. 회원가입 과정에서 다음 순서를 지킵니다.

- username 중복 확인
- 입력값 검증
- Supabase Auth 회원가입
- 생성된 auth.users의 id를 이용하여 profiles 생성
- profiles에는 username, display_name, organization_id,
  recovery_question, recovery_hint, email 등을 저장
- 신규 일반 회원의 role은 임의로 선택할 수 없도록 합니다.
- 회원가입 화면에서는 STUDENT/PARENT/TEACHER/SUPER_ADMIN을
  선택하게 하지 않습니다.

8. SUPER_ADMIN은 일반 회원가입으로 만들 수 없습니다.

9. 회원가입 중 profiles 생성이 실패했을 때
Auth 사용자만 남는 상황을 고려하여 오류 처리를 구현합니다.
가능하면 안전하게 정리할 수 있는 방법을 검토하되
브라우저에서 service_role key를 사용하지 않습니다.

10. 현재 개발 단계에서는 먼저
Supabase client + 회원가입 + 로그인 + 인증상태 유지까지만 구현합니다.

word_books 조회는 인증이 정상적으로 동작한 후 진행합니다.

구현을 시작하기 전에
Supabase의 이메일 확인 설정 때문에 가입/로그인에 문제가 생기지 않는지 확인하고,
필요한 Supabase Dashboard 설정이 있다면 먼저 알려주세요.

위 조건을 반영하여 구현을 시작해주세요.





인증 테스트 결과입니다.

- Supabase Confirm Email: OFF
- 테스트 학생 회원가입: 성공
- profiles 생성: 성공
- 로그아웃 후 test001 로그인: 성공
- F5 후 세션 유지: 성공

profiles에는 현재 다음과 같이 정상 등록되어 있습니다.

관리자:
username = dev.themouse
display_name = 관리자
role = SUPER_ADMIN
organization_id = NULL
can_manage_rounds = TRUE

학생:
username = test001
display_name = 테스트001학생
role = STUDENT
organization_id = 1723e057-5aab-4f48-a1fd-fe1fa0a9fa97
can_manage_rounds = FALSE


그런데 현재 관리자 Auth 계정만 실제 이메일:
dev.themouse@gmail.com

일반 사용자는:
username@no-email.cambridge

형태의 synthetic email을 Auth에 사용하고 있습니다.

로그인 UI는 아이디 + 비밀번호만 사용하기 때문에
관리자도 일반 사용자와 동일한 synthetic email 구조로 통일하고 싶습니다.

현재 관리자 Auth 계정의 이메일을
dev.themouse@no-email.cambridge
로 변경하고,
실제 이메일 dev.themouse@gmail.com은
profiles.email에 참고용으로 유지하는 방향을 검토해주세요.

중요:
- service_role key를 브라우저 코드에 넣지 마세요.
- 기존 관리자 Auth 사용자 ID와 profiles.id 관계를 깨뜨리면 안 됩니다.
- 기존 SUPER_ADMIN 권한도 유지해야 합니다.
- 관리자 계정을 새로 생성해서 기존 계정을 대체하지 마세요.
- Auth email 변경이 현재 Supabase 설정에서 안전하게 가능한지 먼저 확인하세요.
- 실제 변경이 필요한 경우 제가 Supabase Dashboard에서 직접 할 수 있는 방법을 먼저 알려주세요.

코드를 변경하기 전에 분석 결과와 필요한 Dashboard 작업만 설명해주세요.




관리자 Auth 이메일을 Dashboard에서 직접 변경할 수 없는 것을 확인했습니다.

현재 관리자 계정은 그대로 유지하겠습니다.

관리자:
username = dev.themouse
Auth email = dev.themouse@gmail.com
profiles.role = SUPER_ADMIN
profiles.organization_id = NULL
profiles.can_manage_rounds = TRUE

학생:
username = test001
Auth email = test001@no-email.cambridge

현재 회원가입과 학생 로그인은 정상 작동하고 있으므로 기존 인증 구조를 깨뜨리지 마세요.

중요:
- auth.users를 직접 SQL UPDATE하지 마세요.
- service_role 또는 Supabase secret key를 브라우저 코드에 넣지 마세요.
- 관리자 계정을 삭제하거나 새로 만들지 마세요.
- 현재 정상 작동하는 학생 회원가입/로그인 기능을 변경하지 마세요.

우선 관리자 로그인 문제를 해결하기 위한 가장 안전하고 단순한 방법을 코드 관점에서 분석해주세요.

코드를 바로 변경하지 말고,
1. 현재 로그인 코드가 username을 어떻게 Auth email로 변환하는지 확인하고
2. 관리자와 일반 사용자의 인증 방식 차이를 어떻게 최소한으로 처리할지 제안해주세요.

그리고 가능하면 지금 단계에서는 관리자 계정을 건드리지 않고 해결하는 방향을 우선 검토해주세요.




분석 결과를 확인했습니다.

다만 profiles를 로그인 전에 username으로 조회하는 방식은 사용하지 않겠습니다.

이유:
- 로그인 전에는 Supabase Auth 세션이 없으므로 현재 RLS에서 profiles 조회가 가능하다는 보장이 없습니다.
- 로그인 전 profiles 조회를 허용하기 위해 RLS를 완화하면 다른 사용자의 email이나 role 정보가 노출될 수 있습니다.
- 특히 profiles.email에는 실제 이메일 주소가 들어 있으므로 인증 전 공개 조회는 피해야 합니다.

따라서 현재 구조에서는 로그인 로직을 다음과 같이 최소 변경해주세요.

[일반 사용자]
username = test001
→ test001@no-email.cambridge
→ 기존과 동일하게 로그인

[관리자]
username = dev.themouse
→ 먼저 dev.themouse@no-email.cambridge 로 로그인 시도
→ 실패하면 dev.themouse@gmail.com 으로 로그인 재시도

관리자는 현재 1명뿐이므로 이 예외처리는 현재 단계에서 허용합니다.

중요:
- profiles를 로그인 전에 조회하지 마세요.
- RLS를 완화하지 마세요.
- auth.users를 변경하지 마세요.
- 관리자 계정을 새로 만들지 마세요.
- service_role / secret key를 브라우저 코드에 넣지 마세요.
- 기존 학생 회원가입/로그인 기능은 변경하지 마세요.
- 관리자 실제 이메일은 UI에 표시하거나 다른 사용자에게 노출하지 마세요.

로그인 성공 후에는 기존 방식대로 현재 로그인한 auth user의 id를 이용해 profiles를 조회하고,
SUPER_ADMIN / STUDENT 등의 권한을 확인하도록 유지해주세요.

또한 로그인 실패 메시지가
'아이디가 존재하지 않습니다'처럼 계정 존재 여부를 알려주는 형태가 되지 않도록
기존의 일반적인 로그인 실패 메시지를 유지해주세요.

코드를 수정하기 전에 현재 AuthForm.tsx의 로그인 부분을 확인하고,
변경 범위를 최소화해서 구현해주세요.

구현 후 npm run build까지 실행해서 성공 여부를 확인해주세요.






이제 인증 기능은 완료된 것으로 판단하고 다음 단계로 넘어갑니다.

우리 서비스의 핵심 데이터인 단어장/회차/단어 구조를 먼저 설계해주세요.

현재 실제 학습지는 다음 형태입니다.

제목: EVERYDAY WORDS
단계: P2

85회
1. Wednesday - 수요일
2. April - 4월
3. fall - 가을
4. well - 잘, 건강한
5. lunch - 점심

86회
1. when - ~할 때, 언제
2. new - 새로운
3. letter - 편지
4. hear - 듣다
5. English - 영어

87회
1. straight - 곧바로, 쭉
2. Thursday - 목요일
3. together - 함께, 같이
4. December - 12월
5. morning - 아침

88회
1. snowman - 눈사람
2. why - 왜
3. join - 합치다, 참가하다
4. really - 정말로, 실제로
5. sick - 아픈

## 서비스 요구사항

1. 사용자는 여러 학원에 속할 수 있는 구조를 지금 당장 만들 필요는 없습니다.
2. 현재는 사용자의 profiles.organization_id로 학원을 구분합니다.
3. 한 학원에는 여러 단어장이 있을 수 있습니다.
4. 단어장은 제목과 단계로 구분합니다.
   예:
   - EVERYDAY WORDS / P2
5. 하나의 단어장에는 여러 회차가 있습니다.
   예:
   - 85회
   - 86회
   - 87회
   - 88회
6. 회차마다 단어가 여러 개 있습니다.
7. 현재는 보통 5개이지만 반드시 5개라고 가정하지 마세요.
8. 단어에는 최소한 다음 정보가 필요합니다.
   - 순번
   - 영어 단어
   - 뜻풀이
9. 회차를 등록한 사용자와 등록일을 기록해야 합니다.
10. 회차 또는 단어를 수정한 사용자와 수정일도 나중에 추적할 수 있어야 합니다.
11. 회차 등록 권한은 role과 별도로 profiles.can_manage_rounds를 사용합니다.
12. 학생이라도 can_manage_rounds = true라면 회차 등록/수정이 가능합니다.
13. SUPER_ADMIN은 전체 학원을 관리할 수 있어야 합니다.
14. 일반 사용자는 자신이 속한 organization의 데이터만 볼 수 있어야 합니다.
15. 문제 관리 테이블은 만들지 마세요.
16. OCR 관련 테이블도 지금은 만들지 마세요.
17. 문제 출제 이력이나 학습 이력도 지금은 만들지 마세요.

## 중요한 요구사항

처음부터 과도하게 설계하지 마세요.

현재 서비스에 실제로 필요한 최소 테이블 구조를 제안해주세요.

특히 기존 profiles / organizations 테이블과 어떻게 연결되는지 설명해주세요.

가능하면 다음 형태를 기준으로 검토해주세요.

organizations
    ↓
word_books
    ↓
word_rounds
    ↓
words

하지만 이것이 최선이 아니라면 이유를 설명하고 다른 구조를 제안해주세요.

## 작업 방식

아직 SQL을 실행하지 마세요.

먼저 다음을 알려주세요.

1. 필요한 테이블 목록
2. 각 테이블의 컬럼
3. PK / FK 관계
4. UNIQUE 제약
5. 필요한 인덱스
6. 기존 profiles / organizations와의 관계
7. RLS 정책 설계
8. 등록/수정 이력을 어떻게 저장할지
9. 기존 DB와 충돌할 가능성이 있는 부분

특히 RLS를 중요하게 검토해주세요.

설계가 확정되면 그때 SQL을 작성하겠습니다.

코드 수정도 아직 하지 마세요.







좋습니다. 설계 방향은 승인합니다.

다만 SQL을 작성하기 전에 기존 Supabase DB 구조를 실제로 확인해야 합니다.

현재까지 우리가 직접 생성한 기존 테이블과 컬럼을 기준으로 작업해야 하며,
존재한다고 추측해서 테이블이나 컬럼을 새로 만들면 안 됩니다.

## 먼저 해야 할 작업

Supabase 프로젝트의 현재 DB 구조를 확인해주세요.

특히 다음 테이블을 실제로 확인하세요.

- organizations
- profiles
- audit_logs
- word_books
- rounds
- words

각 테이블에 대해 다음을 확인해주세요.

1. 현재 존재 여부
2. 컬럼명
3. 데이터 타입
4. nullable 여부
5. 기본값
6. PK
7. FK
8. UNIQUE 제약
9. 현재 RLS 활성화 여부
10. 현재 RLS 정책
11. 현재 인덱스

가능하다면 Supabase MCP 또는 SQL 조회 기능을 사용해서 실제 DB 구조를 확인하세요.

절대로 추측하지 마세요.

---

## 중요한 권한 설계 수정

앞서 제안한 RLS 설계에서 `auth.role()`을
서비스의 `profiles.role`을 판단하는 용도로 사용하면 안 됩니다.

`auth.role()`은 Supabase의 인증 역할(authenticated 등)을 의미하고,
우리 서비스의 `STUDENT`, `SUPER_ADMIN` 권한과는 다릅니다.

우리 서비스의 권한은 다음 컬럼을 기준으로 판단해야 합니다.

profiles.role
profiles.organization_id
profiles.can_manage_rounds

---

## RLS 구현 방식

RLS 정책마다 profiles를 직접 복잡하게 조회하는 방식보다는
권한 확인용 PostgreSQL 함수를 사용하는 방식을 우선 검토해주세요.

예를 들어 개념적으로 다음과 같은 함수가 필요할 수 있습니다.

- 현재 로그인한 사용자의 organization_id 조회
- 현재 사용자가 SUPER_ADMIN인지 확인
- 현재 사용자가 can_manage_rounds 권한을 가지고 있는지 확인

단, 함수의 정확한 구현은 현재 profiles 테이블 구조와 기존 RLS 정책을 확인한 후 결정하세요.

특히 profiles 자체의 RLS와 함수가 서로 재귀적으로 호출되는 문제가 없는지 확인해주세요.

필요하다면 SECURITY DEFINER 함수를 사용하되,
search_path와 권한 상승 가능성을 안전하게 처리해주세요.

---

## 데이터 구조

최종 목표는 다음 관계입니다.

organizations
    ↓
word_books
    ↓
rounds
    ↓
words

### word_books

필요한 데이터:

- id
- organization_id
- title
- level
- created_by
- updated_by
- created_at
- updated_at

UNIQUE:

(organization_id, title, level)

### rounds

필요한 데이터:

- id
- word_book_id
- round_number
- created_by
- updated_by
- created_at
- updated_at

UNIQUE:

(word_book_id, round_number)

### words

필요한 데이터:

- id
- round_id
- word_order
- english
- meaning
- created_by
- updated_by
- created_at
- updated_at

UNIQUE:

(round_id, word_order)

---

## 권한

SUPER_ADMIN:

- 모든 학원 데이터 조회 가능
- 모든 학원 데이터 관리 가능

일반 사용자:

- 자신의 organization_id 데이터만 조회 가능

can_manage_rounds = true:

- 자신의 학원에서 단어장/회차/단어를 등록 및 수정할 수 있음

중요:

role이 STUDENT이어도 can_manage_rounds = true라면 등록/수정 가능해야 합니다.

즉,

role과 can_manage_rounds는 서로 독립적인 권한입니다.

---

## 삭제 권한

현재 요구사항에는 삭제 기능이 없습니다.

따라서 지금은 DELETE RLS 정책을 만들지 않는 방향을 우선 검토해주세요.

나중에 삭제 기능이 필요할 때 별도로 설계하겠습니다.

---

## audit_logs

현재 요구사항에서는

created_by
created_at
updated_by
updated_at

만으로도 누가 등록하고 마지막으로 수정했는지 알 수 있습니다.

상세 변경 이력(old_data/new_data)은 당장 필요하지 않습니다.

따라서 audit_logs를 이번 단계에서 반드시 확장하거나 사용하지 않아도 됩니다.

---

## 매우 중요한 작업 순서

아직 SQL을 실행하지 마세요.

먼저 현재 DB를 확인하고 다음 내용을 보고해주세요.

1. 현재 실제 테이블 구조
2. 우리가 추가해야 하는 테이블
3. 기존 테이블에 추가해야 하는 컬럼
4. 추가/수정해야 하는 제약조건
5. 추가해야 하는 인덱스
6. 권한 확인 함수가 필요한지
7. RLS 정책 설계
8. 기존 데이터에 영향을 주는 변경이 있는지
9. 실행 순서를 어떻게 해야 안전한지

그 다음 제가 검토할 수 있도록 SQL 실행 계획을 제시해주세요.

검토가 끝난 후에만 실제 SQL을 작성하겠습니다.

절대로 기존 테이블을 DROP하거나 데이터를 삭제하지 마세요.






좋습니다.

지금은 절대로 DB를 변경하지 말고,
현재 Supabase DB의 제약조건과 RLS 상태를 읽기 전용으로 확인해주세요.

앞서 확인한 다음 테이블을 대상으로 합니다.

- organizations
- profiles
- audit_logs
- word_books
- rounds
- words

## 1. 컬럼 상세 정보

information_schema.columns에서 다음 정보를 조회해주세요.

- table_name
- column_name
- data_type
- is_nullable
- column_default

대상 테이블은 위 6개입니다.

---

## 2. PK / FK / UNIQUE / CHECK 제약조건

pg_catalog 또는 information_schema를 사용하여 다음을 확인해주세요.

- PRIMARY KEY
- FOREIGN KEY
- UNIQUE
- CHECK

각 제약조건에 대해 가능하면 다음을 보여주세요.

- 테이블명
- 제약조건명
- 제약조건 종류
- 관련 컬럼
- 참조 대상 테이블
- 참조 대상 컬럼

특히 다음 관계를 정확하게 확인해주세요.

profiles.organization_id
word_books.organization_id

word_books.created_by
word_books.updated_by

rounds.word_book_id
rounds.created_by
rounds.updated_by

words.round_id
words.created_by
words.updated_by

---

## 3. UNIQUE 확인

특히 다음 UNIQUE가 실제로 존재하는지 확인해주세요.

profiles
- username

word_books
- (organization_id, title, level)

rounds
- (word_book_id, round_number)

words
- (round_id, word_order)

존재하지 않는다면 "없음"이라고 명확하게 표시해주세요.

---

## 4. 인덱스 확인

pg_indexes 등을 사용해서 다음 테이블의 현재 인덱스를 전부 보여주세요.

- profiles
- organizations
- word_books
- rounds
- words

각 인덱스의:

- 이름
- 대상 테이블
- 인덱스 정의

를 보여주세요.

중복 인덱스를 새로 만들지 않도록 현재 상태를 정확히 확인하는 것이 목적입니다.

---

## 5. RLS 활성화 여부

각 테이블에 대해 RLS가 활성화되어 있는지 확인해주세요.

대상:

- organizations
- profiles
- audit_logs
- word_books
- rounds
- words

결과를 다음처럼 정리해주세요.

table | rls_enabled

---

## 6. 현재 RLS 정책

pg_policies를 사용하여 다음 테이블의 현재 정책을 모두 조회해주세요.

- organizations
- profiles
- audit_logs
- word_books
- rounds
- words

각 정책에 대해 다음 정보를 보여주세요.

- tablename
- policyname
- permissive
- roles
- cmd
- qual
- with_check

특히 SELECT / INSERT / UPDATE / DELETE 정책을 구분해서 설명해주세요.

---

## 7. 중요

이번 작업에서는 절대로 다음 작업을 하지 마세요.

- CREATE TABLE
- ALTER TABLE
- DROP
- DELETE
- UPDATE
- INSERT
- CREATE POLICY
- DROP POLICY
- CREATE FUNCTION
- DROP FUNCTION

즉, 완전한 READ ONLY 작업만 수행하세요.

현재 DB 상태를 확인하는 것이 목적입니다.

---

## 8. 결과를 보기 쉽게 정리

최종 결과는 다음 순서로 보고해주세요.

### A. 컬럼 구조
### B. PK / FK
### C. UNIQUE
### D. INDEX
### E. RLS 활성화 상태
### F. 현재 RLS 정책
### G. 현재 상태에서 부족한 부분
### H. 변경이 필요한 부분

특히 마지막에는 다음처럼 명확하게 판단해주세요.

- 이미 되어 있음
- 추가 필요
- 수정 필요
- 건드리면 안 됨

그리고 아직 SQL 변경문은 작성하지 마세요.

제가 결과를 검토한 후 다음 단계에서 실제 변경 SQL을 작성하도록 하겠습니다.








-- =========================================================
-- 1. 컬럼 구조
-- =========================================================
select
    table_name,
    column_name,
    data_type,
    is_nullable,
    column_default
from information_schema.columns
where table_schema = 'public'
  and table_name in (
      'organizations',
      'profiles',
      'audit_logs',
      'word_books',
      'rounds',
      'words'
  )
order by table_name, ordinal_position;


-- =========================================================
-- 2. PK / FK / UNIQUE / CHECK
-- =========================================================
select
    tc.table_name,
    tc.constraint_name,
    tc.constraint_type,
    kcu.column_name,
    ccu.table_name as referenced_table,
    ccu.column_name as referenced_column
from information_schema.table_constraints tc
left join information_schema.key_column_usage kcu
    on tc.constraint_name = kcu.constraint_name
    and tc.table_schema = kcu.table_schema
left join information_schema.constraint_column_usage ccu
    on tc.constraint_name = ccu.constraint_name
    and tc.table_schema = ccu.table_schema
where tc.table_schema = 'public'
  and tc.table_name in (
      'organizations',
      'profiles',
      'audit_logs',
      'word_books',
      'rounds',
      'words'
  )
order by
    tc.table_name,
    tc.constraint_type,
    tc.constraint_name;


-- =========================================================
-- 3. 인덱스
-- =========================================================
select
    schemaname,
    tablename,
    indexname,
    indexdef
from pg_indexes
where schemaname = 'public'
  and tablename in (
      'organizations',
      'profiles',
      'audit_logs',
      'word_books',
      'rounds',
      'words'
  )
order by tablename, indexname;


-- =========================================================
-- 4. RLS 활성화 여부
-- =========================================================
select
    schemaname,
    tablename,
    rowsecurity as rls_enabled,
    forcerowsecurity as force_rls
from pg_tables
where schemaname = 'public'
  and tablename in (
      'organizations',
      'profiles',
      'audit_logs',
      'word_books',
      'rounds',
      'words'
  )
order by tablename;


-- =========================================================
-- 5. 현재 RLS 정책
-- =========================================================
select
    schemaname,
    tablename,
    policyname,
    permissive,
    roles,
    cmd,
    qual,
    with_check
from pg_policies
where schemaname = 'public'
  and tablename in (
      'organizations',
      'profiles',
      'audit_logs',
      'word_books',
      'rounds',
      'words'
  )
order by tablename, policyname;